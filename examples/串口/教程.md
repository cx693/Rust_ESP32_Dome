# ESP32-S3 UART 中断接收详解

> **作者：CXi**
> **开发板：ESP32-S3R8N8 嘉立创**
> **语言：Rust (esp-hal)**

---

## 一、项目简介

本项目演示如何在 ESP32-S3 上使用 **Rust** 实现 UART 串口中断接收数据。

**核心知识点：**
- UART 外设配置
- 中断处理机制
- 全局共享状态管理（Mutex + RefCell）
- 主循环与中断的数据交互

---

## 二、硬件连接

| 功能 | ESP32-S3 引脚 | 连接目标 |
|------|--------------|----------|
| UART1 TX | GPIO17 | USB-TTL RX |
| UART1 RX | GPIO18 | USB-TTL TX |
| GND | GND | USB-TTL GND |

**接线示意：**
```
ESP32-S3          USB-TTL 转换器
  GPIO17  ───────►  RX
  GPIO18  ◄───────  TX
  GND     ────────  GND
```

---

## 三、项目结构

```
串口/
├── Cargo.toml
└── src/
    ├── lib.rs        # 库文件（#![no_std]）
    └── bin/
        └── main.rs   # 主程序
```

---

## 四、完整代码

```rust
#![no_main]
#![no_std]

// ==================== ESP32 UART 中断接收示例 ====================
// 功能：通过 UART1 中断接收串口数据，存入缓冲区后在主循环中处理
// 引脚：TX = GPIO17, RX = GPIO18
// 波特率：115200
// ================================================================

use core::{cell::RefCell, fmt::Write};

use critical_section::Mutex;
use esp_bootloader_esp_idf as _;
use esp_hal::{
    handler,
    main, ram,
    uart::{Config, RxConfig, Uart, UartInterrupt},
};
use panic_rtt_target as _;
use rtt_target::{rprintln, rtt_init_print};

esp_bootloader_esp_idf::esp_app_desc!();

// -------------------- 全局共享资源 --------------------
// Mutex<RefCell<T>> 是嵌入式 Rust 中跨中断/主循环共享状态的标准模式
// - Mutex: 临界区保护，防止中断和主循环同时访问
// - RefCell: 运行时借用检查，允许内部可变性

/// UART 实例（全局共享，中断和主循环都需要访问）
static SERIAL: Mutex<RefCell<Option<Uart<'_, esp_hal::Blocking>>>> = Mutex::new(RefCell::new(None));

/// 接收数据缓冲区（128 字节）
static RX_BUF: Mutex<RefCell<[u8; 128]>> = Mutex::new(RefCell::new([0u8; 128]));

/// 接收到的数据长度（0 表示无新数据）
static RX_LEN: Mutex<RefCell<usize>> = Mutex::new(RefCell::new(0));

// -------------------- 主函数 --------------------
#[main]
fn main() -> ! {
    // 初始化 RTT 调试输出（替代串口打印，通过 J-Link/调试器查看）
    rtt_init_print!();
    rprintln!("中断 串口");

    // 初始化 ESP32 HAL（时钟、GPIO 等底层外设）
    let peripherals = esp_hal::init(esp_hal::Config::default());

    // UART 接收配置
    // - fifo_full_threshold: FIFO 满 30 字节时触发中断
    // - timeout: 接收超时 10 个字符时间后触发中断（处理不完整数据包）
    let rx_config = RxConfig::default()
        .with_fifo_full_threshold(30)
        .with_timeout(10);

    // UART 总配置：波特率 115200
    let config = Config::default().with_baudrate(115200).with_rx(rx_config);

    // 创建 UART1 实例，绑定 TX/RX 引脚
    let mut uart = Uart::new(peripherals.UART1, config)
        .unwrap()
        .with_rx(peripherals.GPIO18)  // 接收引脚
        .with_tx(peripherals.GPIO17); // 发送引脚

    // 注册中断处理函数
    uart.set_interrupt_handler(uart_interrupt_handler);

    // 在临界区中开启中断并保存 UART 实例到全局变量
    critical_section::with(|cs| {
        // 启用两种中断：FIFO 满 和 接收超时
        uart.listen(UartInterrupt::RxFifoFull | UartInterrupt::RxTimeout);
        // 将 UART 实例移入全局变量（所有权转移）
        SERIAL.borrow_ref_mut(cs).replace(uart);
    });

    rprintln!("UART1 已就绪 (TX=GPIO17, RX=GPIO18, 115200)");
    rprintln!("等待接收数据...");

    // -------------------- 主循环 --------------------
    // 轮询检查是否有新数据到达（由中断设置 RX_LEN）
    loop {
        let len = critical_section::with(|cs| {
            let len = *RX_LEN.borrow_ref(cs);
            if len > 0 {
                // 读取缓冲区数据
                let buf = RX_BUF.borrow_ref(cs);
                let strdata = core::str::from_utf8(&buf[..len]).unwrap_or("");
                rprintln!("main 收到 {} 字节: {:?}", len, &buf[..len]);
                rprintln!("{}", strdata);
            }
            len
        });

        // 处理完毕，清零长度标记（通知中断可覆盖缓冲区）
        if len > 0 {
            critical_section::with(|cs| {
                *RX_LEN.borrow_ref_mut(cs) = 0;
            });
        }
    }
}

// -------------------- UART 中断处理函数 --------------------
// #[handler] 标记为中断处理函数
// #[ram] 将函数放入 RAM 执行（比 Flash 快，中断对延迟敏感）
#[handler]
#[ram]
fn uart_interrupt_handler() {
    critical_section::with(|cs| {
        let mut serial = SERIAL.borrow_ref_mut(cs);
        if let Some(serial) = serial.as_mut() {
            let mut buf = [0u8; 128];

            // 从 UART FIFO 读取数据到临时缓冲区
            if let Ok(cnt) = serial.read_buffered(&buf) && cnt > 0 {
                // 将数据复制到全局缓冲区
                let mut rx_buf = RX_BUF.borrow_ref_mut(cs);
                rx_buf[..cnt].copy_from_slice(&buf[..cnt]);
                // 设置数据长度（主循环会检查此值）
                *RX_LEN.borrow_ref_mut(cs) = cnt;
            }

            // 清除中断标志（必须！否则中断会持续触发）
            serial.clear_interrupts(UartInterrupt::RxFifoFull | UartInterrupt::RxTimeout);
        }
    })
}
```

---

## 五、代码详解

### 5.1 全局共享状态

在嵌入式系统中，中断和主循环需要共享数据。Rust 的标准做法是：

```rust
static SERIAL: Mutex<RefCell<Option<Uart<'_>>>> = Mutex::new(RefCell::new(None));
```

**为什么需要 Mutex + RefCell？**

| 组件 | 作用 | 类比 |
|------|------|------|
| `Mutex` | 临界区保护，禁止中断嵌套 | 给资源加锁 |
| `RefCell` | 运行时借用检查，允许修改 | 内部可变性 |
| `Option` | 表示值可能存在或不存在 | 延迟初始化 |

---

### 5.2 UART 配置

```rust
let rx_config = RxConfig::default()
    .with_fifo_full_threshold(30)  // FIFO 满 30 字节触发中断
    .with_timeout(10);             // 超时 10 字符时间触发中断
```

**两种中断触发条件：**

1. **FIFO 满中断**：当接收 FIFO 积累到 30 字节时触发
   - 优点：批量处理，效率高
   - 缺点：数据量不足 30 字节时不触发

2. **超时中断**：当接收 FIFO 有数据但未满，且超过 10 个字符时间无新数据时触发
   - 优点：能处理小数据包
   - 缺点：有延迟

**组合使用**：两种方式配合，既能高效处理大数据，也能及时响应小数据。

---

### 5.3 中断处理流程

```
┌─────────────────────────────────────────────────────────┐
│                    数据接收流程                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  UART 硬件 ──► FIFO 缓冲 ──► 触发中断                    │
│                                 │                       │
│                                 ▼                       │
│                    ┌────────────────────────┐            │
│                    │  uart_interrupt_handler │            │
│                    └────────────────────────┘            │
│                                 │                       │
│                                 ▼                       │
│                    读取 FIFO → 存入 RX_BUF               │
│                    设置 RX_LEN = 数据长度                 │
│                    清除中断标志                           │
│                                 │                       │
│                                 ▼                       │
│                    ┌────────────────────────┐            │
│                    │      主循环检测        │            │
│                    └────────────────────────┘            │
│                                 │                       │
│                                 ▼                       │
│                    RX_LEN > 0 ? ──► 处理数据              │
│                                 │                       │
│                                 ▼                       │
│                    RX_LEN = 0 （清零，允许新数据）        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 5.4 关键属性说明

```rust
#[handler]  // 标记为中断处理函数
#[ram]      // 将函数放入 RAM 执行
```

| 属性 | 作用 | 原因 |
|------|------|------|
| `#[handler]` | 告诉编译器这是中断处理函数 | 中断需要特殊调用约定 |
| `#[ram]` | 将代码放入 RAM 而非 Flash | RAM 访问更快，降低中断延迟 |

---

## 六、编译与烧录

### 6.1 环境准备

```bash
# 安装 Rust 嵌入式工具链
rustup target add xtensa-esp32s3-none-elf
cargo install espflash

# 克隆项目
git clone <仓库地址>
cd 串口
```

### 6.2 编译

```bash
cargo build --release
```

### 6.3 烧录

```bash
# 按住 BOOT 按钮，然后执行
cargo run --release
```

---

## 七、运行效果

1. 连接 USB-TTL 转换器到电脑
2. 打开串口助手（波特率 115200）
3. 发送任意数据

**RTT 调试输出：**
```
中断 串口
UART1 已就绪 (TX=GPIO17, RX=GPIO18, 115200)
等待接收数据...
main 收到 5 字节: [72, 101, 108, 108, 111]
Hello
```

---

## 八、常见问题

### Q1: 为什么用 RTT 而不是串口打印？

**答**：ESP32-S3 只有一个 UART0 用于调试输出，UART1 用于通信。RTT 通过调试器（J-Link）输出日志，不占用 UART 资源。

### Q2: 为什么需要清除中断标志？

**答**：不清除标志，中断会持续触发，导致程序卡在中断处理函数中。

### Q3: 为什么用 `critical_section::with`？

**答**：防止中断和主循环同时访问共享数据导致数据竞争。临界区会临时禁用中断。

---

## 九、扩展学习

1. **添加发送功能**：在中断中实现数据回传
2. **使用 DMA**：大数据量传输时使用 DMA 提高效率
3. **多串口支持**：同时使用 UART0 和 UART1

---

## 十、参考资源

- [esp-hal 官方文档](https://docs.rs/esp-hal)
- [esp-hal GitHub 仓库](https://github.com/esp-rs/esp-hal)
- [ESP32-S3 技术参考手册](https://www.espressif.com/sites/default/files/documentation/esp32-s3_technical_reference_manual_en.pdf)

---

## 十一、总结

本项目通过一个完整的 UART 中断接收示例，展示了：

1. **ESP32 HAL 的基本使用**：外设初始化、配置、中断注册
2. **嵌入式 Rust 的状态管理**：Mutex + RefCell 模式
3. **中断处理的最佳实践**：临界区保护、标志清除、RAM 执行

掌握这些知识点，可以举一反三实现其他外设的中断处理。

---

> **作者：CXi**
> **日期：2025**
> **未经许可，请勿转载**
